<h2 style="
  font-family: 'Nunito', sans-serif;
  font-size: 1.8rem;
  font-weight: 800;
  color: #a855f7;
  margin: 1.5rem 0 1.25rem;
  letter-spacing: 0.02em;
  text-align: center;
">Welcome, <%= current_user.proper_name %> 🍦</h2>

<%= react_component("EmployeeHome", { employeeId: current_user.id }) %>