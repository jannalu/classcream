<%= form_with model: store, local: true do |f| %>
  <% if store.errors.any? %>
    <div class="alert alert-danger">
      <p>Please review the problems below</p>
      <% store.errors.full_messages.each do |msg| %>
        <p><%= msg %></p>
      <% end %>
    </div>
  <% end %>

  <div class="cc-form-group">
    <%= f.label :name, "Name", class: "cc-label" %>
    <%= f.text_field :name, class: "cc-input" %>
  </div>
  <div class="cc-form-group">
    <%= f.label :street, "Street", class: "cc-label" %>
    <%= f.text_field :street, class: "cc-input" %>
  </div>
  <div class="cc-form-group">
    <%= f.label :city, "City", class: "cc-label" %>
    <%= f.text_field :city, class: "cc-input" %>
  </div>
  <div class="cc-form-group">
    <%= f.label :state, "State", class: "cc-label" %>
    <%= f.text_field :state, class: "cc-input" %>
  </div>
  <div class="cc-form-group">
    <%= f.label :zip, "Zip", class: "cc-label" %>
    <%= f.text_field :zip, class: "cc-input" %>
  </div>
  <div class="cc-form-group">
    <%= f.label :phone, "Phone", class: "cc-label" %>
    <%= f.text_field :phone, class: "cc-input" %>
  </div>
  <div class="cc-form-group form-check">
    <%= f.check_box :active, class: "form-check-input" %>
    <%= f.label :active, class: "form-check-label" %>
  </div>
  <%= f.submit class: "btn-cc" %>
<% end %>